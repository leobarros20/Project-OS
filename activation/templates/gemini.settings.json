{
  "_project-os": "v0.6 id:activation-gemini — merged into .gemini/settings.json by `project-os init`. Matcher omitted on purpose: a narrow matcher misses resume and clear. Alternative: `gemini hooks migrate --from-claude` after the Claude config is in place.",
  "hooks": {
    "SessionStart": [
      { "hooks": [ { "type": "command", "command": "sh .project-os/shim.sh gemini", "timeout": 10 } ] }
    ]
  }
}
